# Legacy Vite Prototype

This folder is a non-production migration source for Phase 2.

The production frontend now lives in `apps/web` as the Next.js application. Keep
this Vite prototype only as a temporary visual reference until Next.js parity is
accepted. Do not add new production features here.
